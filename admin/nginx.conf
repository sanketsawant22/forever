server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    # SPA fallback for ALL admin routes
    location / {
        try_files $uri /index.html;
    }

    location /assets/ {
        try_files $uri =404;
    }
}
